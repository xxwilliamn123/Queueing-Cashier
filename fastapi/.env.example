# USB printer IDs (hex)
POS58_VID=0x0FE6
POS58_PID=0x811E

# FastAPI server (host must be Windows for USB + Zadig; Docker cannot see the printer)
FASTAPI_HOST=127.0.0.1
FASTAPI_PORT=8000

# Optional: lock CORS to your kiosk app URL
KIOSK_ORIGIN=http://127.0.0.1:8000
